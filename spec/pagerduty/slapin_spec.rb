require 'spec_helper'

describe Pagerduty::Slapin do
  it 'has a version number' do
    expect(Pagerduty::Slapin::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
